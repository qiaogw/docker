version: '3'
services:
  gitlab:
    image:  'caeret/gitlab-ce-zh:12.1.0'
    restart: always
    container_name: gitlab
    hostname: gitlab     
    privileged: true #解决权限    
    environment:
      TZ: 'Asia/Shanghai'
      GITLAB_OMNIBUS_CONFIG: |
        external_url 'http://127.0.0.1'
        gitlab_rails['time_zone'] = 'Asia/Shanghai'
        gitlab_rails['smtp_enable'] = true
        gitlab_rails['smtp_address'] = "smtp.exmail.qq.com"
        gitlab_rails['smtp_port'] = 465
        gitlab_rails['smtp_user_name'] = "lzqy@onstream-cloud.com"
        gitlab_rails['smtp_password'] = "lzqy@4001"
        gitlab_rails['smtp_domain'] = "smtp.qq.com"
        gitlab_rails['smtp_authentication'] = "login"
        gitlab_rails['smtp_enable_starttls_auto'] = true
        gitlab_rails['smtp_tls'] = true
        gitlab_rails['gitlab_email_from'] = 'lzqy@onstream-cloud.com'
        user["git_user_email"] = "lzqy@onstream-cloud.com"
        redis['enable'] = false
        gitlab_rails['redis_host'] = 'redis'
        gitlab_rails['redis_port'] = 6379
        postgresql['enable'] = false
        gitlab_rails['db_adapter'] = 'postgresql'
        gitlab_rails['db_encoding'] = 'utf8'
        gitlab_rails['db_host'] = 'postgresql'
        gitlab_rails['db_port'] = 5432
        gitlab_rails['db_username'] = 'pgadmin'
        gitlab_rails['db_password'] = 'pgadmin123456'
        gitlab_rails['db_database'] = 'postgres'
    ports:
      - '10080:80'
      - '10443:443'
      - '10022:22'
    volumes:
      - /dockerdata/gitlab/config:/etc/gitlab:Z
      - /dockerdata/gitlab/data:/var/opt/gitlab:Z
      - /dockerdata/gitlab/logs:/var/log/gitlab:Z
    networks:
      - dev-net
networks:
  dev-net:
    external: true